class Fac {
	int a; 
	
	public int ComputeFac(int num) {
		int[] num_aux;

		if (a < 1 && a < 3)
			a = num_aux + 1;
		else
			num_aux = num * (this.CkkjhomputeFac(num-1));

		return num_aux;
	}

	public int AnotherMethod(int[] a) {
		int c;

		// c = 3;

		return c;
	}

	public static void main(String[] a){
		int b ;
		io.println(new Fac().ComputeFac(10)); // assuming the existence
		// of the classfile io.class
	}
}
